name: Auto Merge main to dev branch (with submodules)

on:
  push:
    branches:
      - main

jobs:
  merge:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout main branch
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          submodules: true

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
      - name: Ensure dev branch exists
        run: |
          if git ls-remote --exit-code --heads origin dev-7.0.0-a-1; then
            echo "Branch exists on remote"
            git fetch origin dev-7.0.0-a-1-
            git checkout dev-7.0.0-a-1
          else
            echo "Branch does not exist, creating it"
            git checkout -b dev-7.0.0-a-1
          fi
          

      - name: Force replace dev branch with main branch contents (including submodules)
        run: |
          set -e
          git reset --hard origin/main
          git submodule update --init --recursive
          git add .
          git commit -m "Force sync dev branch with main branch (including submodules)" || echo "No changes to commit"

      - name: Push changes to dev
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git push https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git dev-7.0.0-a-1 --force
